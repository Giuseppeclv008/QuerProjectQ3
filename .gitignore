# Data — never commit (GBs)
*.zip
*.csv

# sweep evidence (82 lines), committed by Plan 5 Task 5:
!bench/results.csv
# sweep evidence for the CUDA benchmark, pasted back from the target machine:
!bench/results_cuda.csv
!bench/results_cuda_stages.csv
# Parquet-vs-DuckDB evidence. Every figure in docs/bench/results.md has to
# recompute from a committed artifact, and *.csv above silently swallowed two
# of these -- listed one by one rather than un-ignoring the directory, because
# the sweeps also drop scratch CSVs beside them.
!bench/read_results.csv
!bench/read_results_with_sort.csv
!bench/parquet-comparison/write_raw_perday.csv
!bench/parquet-comparison/run_bench_smoke.csv
!bench/parquet-comparison/run_bench_smoke_posthoist.csv
telemetry_*/
*.duckdb
*.parquet
*.wal

# Build
/build/
/build-bench/
/build-full/
/build-plan/
/build-sweep/
cmake-build-*/
*.o
*.obj
*.a

# Python
__pycache__/
*.pyc
.venv/
venv/

# Claude local
.claude/settings.local.json

# Diagrams
*.jar

# OS
.DS_Store

# Personal files
pers_*

/build-verify/
/build-gpu-tests/

# Secrets. The project reads ANTHROPIC_API_KEY from the environment and has
# never used a .env file -- these entries exist so that creating one cannot
# commit a key by accident.
.env
.env.*
!.env.example
*.pem
